# OpenSpec 决策记录 — {{date}}

## 结论

- feature_id: {{feature_id}}
- mode: structure-only | formal-change
- change_id: {{change_id}}
- decision: 本需求是否需要正式 OpenSpec change

## 判定依据

- 是否新增能力:
- 是否破坏 API / 数据结构:
- 是否改变架构模式:
- 是否涉及安全 / 权限 / 性能语义变化:
- 是否仅做需求结构化和验收边界抽取:

## 执行记录

- 已读取 `openspec/AGENTS.md`: 是 | 否
- structure-only 输出:
  - R 编号:
  - 场景:
  - 验收边界:
- formal-change 输出:
  - authoritative_location: `docs/01-features/{{feature_id}}-功能名/openspec/changes/{{change_id}}/`
  - proposal:
  - tasks:
  - specs:
  - root_mirror_used_for_cli: 是 | 否
  - validate command:
  - validate result:

## 关键结论

- 
