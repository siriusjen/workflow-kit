# 实现记录 2026-05-29 T08

## 任务

T08 簿记命令降摩擦。

## 代码根

`/Users/zhangweihong/Documents/module/wk-f01-workflow-automation`

## 改动

- `docs/.workflow/scripts/stage_gates.py` 支持 `subagent-done` 在单个未完成同名派遣时省略 `dispatch_id`。
- 当存在多个未完成同名派遣时，`subagent-done` 仍强制提供 `dispatch_id` 并列出候选，避免误匹配。
- `stage_gates.py step-done` CLI 支持省略步骤名：`stage_gates.py step-done <FID> <结论JSON>`，脚本从当前 `in_progress_step.step` 推断步骤名。
- `AGENTS.md`、`CLAUDE.md`、`docs/.workflow/工作流规范.md` 更新最短安全调用说明。
- `docs/.workflow/scripts/tests/test_workflow_gates.py` 增加单派遣省略、双派遣拒绝、省略 step 名关闭当前步骤测试。

## TDD 记录

- RED: 新增测试后，`test_workflow_gates.py` 失败 2 项；失败原因是 `subagent-done` 校验仍要求 `dispatch_id`，`step-done` CLI 仍要求显式步骤名。
- GREEN: 放宽 `dispatch_id` 字段为可选、增加唯一未完成派遣匹配逻辑，并调整 `step-done` 入口参数解析后，`test_workflow_gates.py` 31 tests 通过。
- 回归: `test_workflow_def.py` 7 tests 通过，`test_install_entry.py` 7 tests 通过，`py_compile` 与 `git diff --check` 通过。

## 验证

- `python3 docs/.workflow/scripts/tests/test_workflow_gates.py` 通过，31 tests。
- `python3 docs/.workflow/scripts/tests/test_workflow_def.py` 通过，7 tests。
- `python3 docs/.workflow/scripts/tests/test_install_entry.py` 通过，7 tests。
- `python3 -m py_compile docs/.workflow/scripts/workflow_common.py docs/.workflow/scripts/workflow_config.py docs/.workflow/scripts/workflow_def.py docs/.workflow/scripts/stage_gates.py docs/.workflow/scripts/validators.py docs/.workflow/scripts/context_packets.py docs/.workflow/scripts/init_feature.py docs/.workflow/scripts/install_entry.py` 通过。
- `git diff --check` 通过。

## 需求映射

- R10: 已覆盖。簿记命令降低了单派遣和当前步骤关闭的重复输入，同时保留多派遣精确匹配约束。

## 结论

T08 完成。常见单线流程下命令更短，存在歧义时仍强制精确参数。
