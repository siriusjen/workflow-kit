# 实现记录 2026-05-29 T01

## 任务

T01 安装入口受管块合并注入。

## 代码根

`/Users/zhangweihong/Documents/module/wk-f01-workflow-automation`

## 改动

- 新增 `docs/.workflow/scripts/install_entry.py`，支持 `--target`、`--only`、`--dry-run`、`--template-dir`。
- 新增 `docs/.workflow/templates/entry/AGENTS.md.tpl` 与 `CLAUDE.md.tpl`，受管块随 `.workflow` 一起迁移。
- 新增 `docs/.workflow/scripts/tests/test_install_entry.py`，覆盖创建、追加、替换、幂等、dry-run、半截标记和模板缺失。
- `AGENTS.md` / `CLAUDE.md` 加入 `WORKFLOW-KIT` 受管块边界，项目覆盖层保留在受管块外。
- `INSTALL.md`、`使用指南.md`、`docs/.workflow/工作流规范.md §11` 改为“复制 `.workflow` 后运行安装注入脚本”的模型，并删除旧模板文件名。

## TDD 记录

- RED: `python3 docs/.workflow/scripts/tests/test_install_entry.py` 失败，原因是 `install_entry.py` 不存在。
- GREEN: 实现安装脚本和模板后，安装测试通过。
- REFACTOR: 补充“替换受管块不得改变标记外字节”的测试，先失败，修复 `upsert_block` 后通过。

## 验证

- `python3 docs/.workflow/scripts/tests/test_install_entry.py` 通过，7 tests。
- `python3 docs/.workflow/scripts/tests/test_workflow_gates.py` 通过，5 tests。
- `rg -n "开发AGENTS|开发CLAUDE" AGENTS.md CLAUDE.md INSTALL.md 使用指南.md docs/.workflow/工作流规范.md docs/.workflow/templates/entry docs/.workflow/scripts` 无输出。
- 临时目录 smoke test 通过：只复制 `docs/.workflow` 后可注入 `AGENTS.md`，二次 `--dry-run` 显示 `No changes.`，目标既有内容保留。

## 需求映射

- R01: 已覆盖。
- R02: 已覆盖。

## 结论

T01 完成。安装模型已从覆盖/不存在模板切换为可迁移受管块 upsert。
