# 规格复核记录 2026-05-29 T05

## 范围

T05 声明式前置条件与 state 元数据。

## 对照需求

- R06: auto gate 前置条件通过统一 evaluator 判断。
- R07: docs_only/worktree/build_config 等运行元数据写入 state，旧 state 缺字段仍可读取。

## 检查项

- OpenSpec、artifact、HTTP、docs-only、cross-validate、RDTV、worktree gate 前置条件已进入 `auto_prereqs`: 通过。
- `validate_auto_prereqs` 根据声明式定义执行，不再按 gate 维护大段硬编码分支: 通过。
- 构建产物校验仍读取 `project_config.json`，默认 Java/Maven/Jar 行为保留: 通过。
- `worktree-created` 仍写入 `workflow_runtime.doc_root` 和 `workflow_runtime.code_worktree_path`: 通过。
- 旧 feature state 缺少 `workflow_runtime` 时可读取并补默认值: 通过。
- 规范文档已说明 `auto_prereqs` 归属和 runtime 兼容: 通过。

## 结论

T05 passed: true
