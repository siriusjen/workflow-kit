# 实现记录 2026-05-29 T03

## 任务

T03 阶段、门禁、子 Agent 定义单一事实源。

## 代码根

`/Users/zhangweihong/Documents/module/wk-f01-workflow-automation`

## 改动

- 新增 `docs/.workflow/workflow_definition.json`。
- 新增 `docs/.workflow/scripts/workflow_def.py`。
- 新增 `docs/.workflow/scripts/tests/test_workflow_def.py`。
- `stage_gates.py` 的 feature stage、bug stage、子 Agent 名称和按阶段约束改为从 `workflow_def.py` 加载。
- `context_packets.py` 的 feature context stage 定义改为从 `workflow_definition.json` 加载。
- `init_feature.py` 的 feature stage display 改为从加载器读取。
- `validators.py` 的 feature stage canonical 映射改为从加载器读取。

## TDD 记录

- RED: `python3 docs/.workflow/scripts/tests/test_workflow_def.py` 失败，原因是 `workflow_def` 模块不存在。
- GREEN: 新增 `workflow_definition.json` 和 `workflow_def.py` 后，`test_workflow_def.py` 4 tests 通过。
- 回归: `test_workflow_gates.py` 12 tests 通过；`test_install_entry.py` 7 tests 通过。

## 验证

- `python3 docs/.workflow/scripts/tests/test_workflow_def.py` 通过，4 tests。
- `python3 docs/.workflow/scripts/tests/test_workflow_gates.py` 通过，12 tests。
- `python3 docs/.workflow/scripts/tests/test_install_entry.py` 通过，7 tests。
- `rg` 检查显示 feature stage、stage display、known subagents 和 subagents-by-stage 的重复手写表已从主要脚本收敛到 loader；剩余 bug context/bug init 表保留为后续独立切片，未改变现有行为。

## 需求映射

- R04: 已覆盖第一阶段等价迁移。

## 结论

T03 完成。单一事实源地基已建立，后续 T04/T05 可继续把 gate validator 和 prereq 声明下沉到同一配置。
