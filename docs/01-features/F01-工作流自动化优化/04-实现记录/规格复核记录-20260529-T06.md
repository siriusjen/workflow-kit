# 规格复核记录 2026-05-29 T06

## 范围

T06 上下文包模板化与缺口提示。

## 对照需求

- R08: 上下文包正文从模板渲染，must-read 缺失时输出待生成清单。

## 检查项

- feature 上下文包使用 `docs/.workflow/templates/上下文包.md.tpl`: 通过。
- bug 上下文包使用 `docs/.workflow/templates/bug上下文包.md.tpl`: 通过。
- feature 包包含 `template: feature-context-packet` 标记: 通过。
- bug 包包含 `template: bug-context-packet` 标记: 通过。
- 缺失 must-read 项进入 `## 待生成/待定位清单`: 通过。
- 原有状态摘要、任务片段、RDTV 片段和最近产物索引保留: 通过。

## 结论

T06 passed: true
