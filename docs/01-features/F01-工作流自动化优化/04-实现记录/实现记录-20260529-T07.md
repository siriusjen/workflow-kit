# 实现记录 2026-05-29 T07

## 任务

T07 公共工具模块与配置 schema。

## 代码根

`/Users/zhangweihong/Documents/module/wk-f01-workflow-automation`

## 改动

- 新增 `docs/.workflow/scripts/workflow_common.py`，提供 `WorkflowJsonError`、`read_json_file()`、`write_json_file()`、`atomic_write_text()`。
- `docs/.workflow/scripts/workflow_config.py` 改为通过公共 JSON helper 读取 `project_config.json`。
- `workflow_config.py` 新增 `WorkflowConfigError`，当 `project_config.json` 非对象、JSON 无效或 `workflow` 字段类型错误时明确失败。
- `workflow_config.py` 对 workflow 配置做 schema 校验：布尔项必须为 boolean，阈值项必须为非负整数，缺失项仍使用默认值。
- `stage_gates.py` 和 `init_feature.py` 捕获 `WorkflowConfigError`，输出清晰中文错误并退出，避免堆栈暴露给日常用户。
- `docs/.workflow/scripts/tests/test_workflow_gates.py` 增加无效配置类型、公共 JSON 读取错误、CLI 友好错误输出测试。

## TDD 记录

- RED: 新增公共 JSON helper 和配置类型校验测试后，`test_workflow_gates.py` 因 `workflow_common` 模块不存在失败。
- GREEN: 新增 `workflow_common.py`，改造 `workflow_config.py` 并在入口脚本捕获配置错误后，`test_workflow_gates.py` 28 tests 通过。
- 回归: `test_workflow_def.py` 7 tests 通过，`test_install_entry.py` 7 tests 通过，`py_compile` 与 `git diff --check` 通过。

## 验证

- `python3 docs/.workflow/scripts/tests/test_workflow_gates.py` 通过，28 tests。
- `python3 docs/.workflow/scripts/tests/test_workflow_def.py` 通过，7 tests。
- `python3 docs/.workflow/scripts/tests/test_install_entry.py` 通过，7 tests。
- `python3 -m py_compile docs/.workflow/scripts/workflow_common.py docs/.workflow/scripts/workflow_config.py docs/.workflow/scripts/workflow_def.py docs/.workflow/scripts/stage_gates.py docs/.workflow/scripts/validators.py docs/.workflow/scripts/context_packets.py docs/.workflow/scripts/init_feature.py docs/.workflow/scripts/install_entry.py` 通过。
- `git diff --check` 通过。

## 需求映射

- R09: 部分完成且满足 T07 第一阶段。公共 JSON helper 已建立，配置 schema 类型错误已明确失败，缺失键默认值仍可见。大规模 state helper 去重留给后续低风险切片。

## 结论

T07 完成。配置读取从静默降级改为明确校验，公共 JSON/atomic write helper 已落地，调用侧保持用户友好错误输出。
