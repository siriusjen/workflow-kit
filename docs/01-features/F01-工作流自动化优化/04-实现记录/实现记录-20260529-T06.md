# 实现记录 2026-05-29 T06

## 任务

T06 上下文包模板化与缺口提示。

## 代码根

`/Users/zhangweihong/Documents/module/wk-f01-workflow-automation`

## 改动

- 新增 `docs/.workflow/templates/上下文包.md.tpl`，作为 feature 上下文包模板。
- 新增 `docs/.workflow/templates/bug上下文包.md.tpl`，作为 bug 上下文包模板。
- `docs/.workflow/scripts/context_packets.py` 增加轻量模板渲染函数，feature/bug 上下文包正文改为从模板生成。
- `expand_must_read_items()` 改为同时返回已定位清单与 `pending` 缺口清单。
- feature 与 bug 上下文包新增 `## 待生成/待定位清单`，把缺失 must-read 项独立列出，便于主 Agent 和子 Agent 先补缺口。
- `docs/.workflow/scripts/tests/test_workflow_gates.py` 增加 feature S6 模板渲染、must-read 缺口提示和 bug B1 模板标记测试。

## TDD 记录

- RED: 新增模板标记与缺口清单测试后，`test_workflow_gates.py` 失败 2 项；失败原因是上下文包仍由 f-string 拼接，且缺失项只混在必须读取清单中。
- GREEN: 新增两个模板文件，改造 `context_packets.py` 使用模板渲染，并输出独立 pending 清单后，`test_workflow_gates.py` 25 tests 通过。
- 回归: `test_workflow_def.py` 7 tests 通过，`test_install_entry.py` 7 tests 通过，`py_compile` 与 `git diff --check` 通过。

## 验证

- `python3 docs/.workflow/scripts/tests/test_workflow_gates.py` 通过，25 tests。
- `python3 docs/.workflow/scripts/tests/test_workflow_def.py` 通过，7 tests。
- `python3 docs/.workflow/scripts/tests/test_install_entry.py` 通过，7 tests。
- `python3 -m py_compile docs/.workflow/scripts/workflow_def.py docs/.workflow/scripts/stage_gates.py docs/.workflow/scripts/validators.py docs/.workflow/scripts/context_packets.py docs/.workflow/scripts/init_feature.py docs/.workflow/scripts/install_entry.py` 通过。
- `git diff --check` 通过。
- 说明: 代码 worktree 中没有 F01 feature 文档目录，直接执行 `context_packets.py build F01 ...` 会返回 `找不到 feature: F01`；模板渲染由临时 feature/bug 单元测试覆盖。主项目目录已刷新 `F01` 当前 S6/T06 上下文包。

## 需求映射

- R08: 已覆盖。上下文包正文由模板渲染，缺失 must-read 项输出为独立待生成/待定位清单。

## 结论

T06 完成。上下文包生成逻辑从内联字符串迁移到模板，缺口提示更直接，后续可继续把 bug stage 定义也迁移到统一声明源。
