# 实现记录 2026-05-29 T04

## 任务

T04 校验器由门禁自动调度。

## 代码根

`/Users/zhangweihong/Documents/module/wk-f01-workflow-automation`

## 改动

- `docs/.workflow/workflow_definition.json` 新增 `gate_validators` 绑定，将 `req-coverage-passed`、`req-cross-validated`、`fact-inheritance-passed`、`rdt-mapping-passed`、`rdtv-mapping-complete` 映射到对应 validator。
- `docs/.workflow/scripts/stage_gates.py` 在 `cmd_auto` 中按 gate 查询 validator，先校验再推进 state；校验失败时保持原阶段和 checklist 不变。
- `docs/.workflow/scripts/validators.py` 移除 feature validators 内部 `_trigger_auto` 副作用，保留 CLI 诊断返回码，并新增 `run_validator()` 供门禁调用。
- `docs/.workflow/scripts/tests/test_workflow_def.py` 覆盖声明式 gate validator 绑定完整性。
- `docs/.workflow/scripts/tests/test_workflow_gates.py` 覆盖 invalid gate 阻断、valid gate 推进，以及 validators 不触发状态转移。

## TDD 记录

- RED: 新增 gate validator 绑定和 validator 无副作用测试后，`python3 docs/.workflow/scripts/tests/test_workflow_def.py` 失败 1 项，`test_workflow_gates.py` 失败 5 项；失败原因是定义只绑定 `rdt-mapping-passed`，且 `req_coverage`、`req_cross_validate`、`fact_inheritance`、`rdtv_closure` 仍调用 `_trigger_auto`。
- GREEN: 补齐 `gate_validators`，移除 validator 内部自动推进逻辑，将推进权收口到 `stage_gates.py auto` 后，`test_workflow_def.py` 5 tests 通过，`test_workflow_gates.py` 20 tests 通过。
- 回归: `test_install_entry.py` 7 tests 通过；`py_compile` 覆盖 `workflow_def.py`、`stage_gates.py`、`validators.py`、`context_packets.py`、`init_feature.py`、`install_entry.py`。

## 验证

- `python3 docs/.workflow/scripts/tests/test_workflow_def.py` 通过，5 tests。
- `python3 docs/.workflow/scripts/tests/test_workflow_gates.py` 通过，20 tests。
- `python3 docs/.workflow/scripts/tests/test_install_entry.py` 通过，7 tests。
- `python3 -m py_compile docs/.workflow/scripts/workflow_def.py docs/.workflow/scripts/stage_gates.py docs/.workflow/scripts/validators.py docs/.workflow/scripts/context_packets.py docs/.workflow/scripts/init_feature.py docs/.workflow/scripts/install_entry.py` 通过。
- `git diff --check` 通过。

## 需求映射

- R05: 已覆盖。validator CLI 只负责诊断和返回通过/失败；状态推进统一由 `stage_gates.py auto` 执行。

## 结论

T04 完成。自动门禁现在拥有 validator 调度和状态推进权，validator 模块可被 import 复用，并消除了 CLI 诊断时隐式修改 state 的风险。
