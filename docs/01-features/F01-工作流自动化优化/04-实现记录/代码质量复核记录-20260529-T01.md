# 代码质量复核 2026-05-29 T01

## 范围

T01 安装入口受管块合并注入。

## 检查项

- 标准库实现，无新增第三方依赖: 通过。
- 错误路径有明确 stderr 和非 0 退出: 通过。
- `upsert_block` 对目标内容只做受管块内替换，标记外内容通过测试锁定: 通过。
- 入口模板随 `.workflow` 迁移，安装命令顺序先创建目标 docs 目录: 通过。
- 现有门禁测试不回退: 通过，`test_workflow_gates.py` 5 tests OK。
- 旧模板名 `开发AGENTS` / `开发CLAUDE` 已从 T01 范围文档中移除: 通过。

## 风险

- `AGENTS.md` / `CLAUDE.md` 的受管块模板包含当前完整入口正文；后续若工作流正文变化，应通过 `install_entry.py` 刷新目标项目入口。

## 结论

passed: true
