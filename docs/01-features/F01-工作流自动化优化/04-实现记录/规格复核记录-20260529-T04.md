# 规格复核记录 2026-05-29 T04

## 范围

T04 校验器由门禁自动调度。

## 对照需求

- R05: validator 函数可被门禁 import 调用，CLI 不再推进 state，`stage_gates auto` 负责校验和转移。

## 检查项

- `gate_validators` 覆盖 S2、S3、S5、S9 的 feature validator gate: 通过。
- `stage_gates.py auto` 在执行状态变更前调用对应 validator: 通过。
- validator 通过时只返回结果和写诊断产物，不再调用外部 `stage_gates.py auto`: 通过。
- validator 失败时自动门禁阻断，state 不推进: 通过。
- RDTV/RDT 生成诊断产物的既有行为保留: 通过。
- 测试覆盖 RED/GREEN 和回归命令: 通过。

## 结论

T04 passed: true
