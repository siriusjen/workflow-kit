# 实现记录 2026-05-29 T05

## 任务

T05 声明式前置条件与 state 元数据。

## 代码根

`/Users/zhangweihong/Documents/module/wk-f01-workflow-automation`

## 改动

- `docs/.workflow/workflow_definition.json` 新增 `auto_prereqs`，集中声明 OpenSpec、artifact、HTTP、docs-only、cross-validate、RDTV、worktree 等自动门禁前置条件。
- `docs/.workflow/scripts/workflow_def.py` 增加 `auto_prereqs` 解析和类型校验，错误信息能定位到具体 gate。
- `docs/.workflow/scripts/stage_gates.py` 将 `validate_auto_prereqs` 改为通用 evaluator，支持 `checklist_true`、`glob_exists`、`latest_glob`、`latest_file_matches`、`recent_files_match`、`artifact_exists`、`build_record_mentions_artifact`、`code_worktree_exists`、formal-change 专项检查。
- `stage_gates.py` 增加 `workflow_runtime` 默认补齐逻辑，旧 state 缺少 `doc_root` 或 `code_worktree_path` 时读取阶段自动补齐空值。
- `docs/.workflow/工作流规范.md` 说明 `auto_prereqs` 是自动门禁前置条件的单一声明位置，并补充旧 state runtime 兼容说明。
- `docs/.workflow/scripts/tests/test_workflow_def.py` 和 `test_workflow_gates.py` 增加声明式 prereq、runtime 默认值、worktree metadata 写入测试。

## TDD 记录

- RED: 新增 `auto_prereqs` 加载和声明式 evaluator 测试后，`test_workflow_def.py` 失败 2 项，`test_workflow_gates.py` 失败 1 项；失败原因是 `WorkflowDefinition` 尚无 `auto_prereqs` 字段，且 `validate_auto_prereqs` 仍为硬编码分支。
- GREEN: 实现 `auto_prereqs` loader、JSON 规则和通用 evaluator 后，`test_workflow_def.py` 7 tests 通过，`test_workflow_gates.py` 23 tests 通过。
- 回归: `test_install_entry.py` 7 tests 通过；`git diff --check` 与 `py_compile` 通过。

## 验证

- `python3 docs/.workflow/scripts/tests/test_workflow_def.py` 通过，7 tests。
- `python3 docs/.workflow/scripts/tests/test_workflow_gates.py` 通过，23 tests。
- `python3 docs/.workflow/scripts/tests/test_install_entry.py` 通过，7 tests。
- `python3 -m py_compile docs/.workflow/scripts/workflow_def.py docs/.workflow/scripts/stage_gates.py docs/.workflow/scripts/validators.py docs/.workflow/scripts/context_packets.py docs/.workflow/scripts/init_feature.py docs/.workflow/scripts/install_entry.py` 通过。
- `git diff --check` 通过。

## 需求映射

- R06: 已覆盖。自动门禁前置条件由统一 evaluator 读取 `workflow_definition.json` 执行。
- R07: 已覆盖。`workflow_runtime` 缺字段时向后兼容补默认值，worktree gate 继续写入 `doc_root` 和 `code_worktree_path`。

## 结论

T05 完成。自动门禁前置条件已从脚本分支迁移到声明式定义，旧 state runtime 元数据兼容读取，现有门禁行为保持回归通过。
