# 规格复核 2026-05-29 T01

## 范围

T01 安装入口受管块合并注入。

## 对照需求

- R01: 安装入口从覆盖改为合并注入。通过。
- R02: 安装脚本来源可迁移。通过。

## 检查项

- 目标入口文件不存在时可创建受管块: 通过，`test_creates_missing_entry_files_from_templates` 覆盖。
- 目标入口文件已有项目内容时保留标记外字节: 通过，`test_appends_block_without_changing_existing_content` 和 `test_replaces_block_without_changing_surrounding_bytes` 覆盖。
- 已有受管块时替换且幂等: 通过，`test_replaces_existing_block_idempotently` 覆盖。
- `--dry-run` 不写文件: 通过。
- 模板缺失和半截标记明确失败: 通过。
- 安装脚本不依赖 workflow-kit 源根目录: 通过，模板位于 `docs/.workflow/templates/entry/`。

## 结论

passed: true
