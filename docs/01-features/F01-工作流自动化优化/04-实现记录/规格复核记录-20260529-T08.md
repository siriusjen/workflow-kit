# 规格复核记录 2026-05-29 T08

## 范围

T08 簿记命令降摩擦。

## 对照需求

- R10: 单派遣 `subagent-done` 可省 `dispatch_id`，多派遣仍强制 `dispatch_id`，`step-done` 可默认当前步骤。

## 检查项

- 单个未完成同名派遣时可省略 `dispatch_id`: 通过。
- 多个未完成同名派遣时省略 `dispatch_id` 会失败并提示候选: 通过。
- 没有唯一未完成派遣时禁止事后补 `subagent-done`: 通过。
- `step-done <FID> <结论JSON>` 可使用当前 `in_progress_step.step`: 通过。
- 无进行中步骤时省略步骤名仍失败: 通过。
- 入口文档和工作流规范已说明最短安全调用: 通过。

## 结论

T08 passed: true
