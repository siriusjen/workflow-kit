# 实现记录 2026-05-29 T09

## 任务

T09 文档语言无关叙事。

## 代码根

`/Users/zhangweihong/Documents/module/wk-f01-workflow-automation`

## 改动

- 新增根目录 `README.md`，明确 `workflow-kit` 是语言无关工作流包，Java/Maven/Jar 只是默认构建验收 profile。
- `docs/README.md` 增加语言无关说明、构建 profile 覆盖入口、默认 Java profile、前端示例、Go 示例。
- `INSTALL.md` 将原有简略示例扩展为完整 JSON 片段。
- `docs/.workflow/工作流规范.md` 在开头增加语言定位说明，并把 S8/S10、HTTP 验收、构建流程中的强 Jar 叙事改为构建产物叙事。
- `使用指南.md` 已在前置版本中包含语言无关说明与前端/Go 示例，本次保留。

## TDD 记录

- RED: 先用 `rg "Java|Maven|Jar|target/|dist/|bin/|project_config|语言无关|Go|前端|frontend"` 扫描公开文档，发现根目录缺 `README.md`，`docs/README.md` 仍缺语言无关和构建示例，规范中仍有部分强 Jar 叙事。
- GREEN: 补齐根 `README.md`、`docs/README.md` 和规范措辞后，grep 结果中的 Java/Jar 均处于默认 profile 语境，前端/Go 示例可见。
- 回归: `test_workflow_gates.py` 31 tests 通过，`test_workflow_def.py` 7 tests 通过，`test_install_entry.py` 7 tests 通过，`git diff --check` 通过。

## 验证

- `rg "Java|Maven|Jar|target/|dist/|bin/|project_config|语言无关|Go|前端|frontend" README.md docs/README.md INSTALL.md 使用指南.md docs/.workflow/工作流规范.md` 已人工复核。
- `python3 docs/.workflow/scripts/tests/test_workflow_gates.py` 通过，31 tests。
- `python3 docs/.workflow/scripts/tests/test_workflow_def.py` 通过，7 tests。
- `python3 docs/.workflow/scripts/tests/test_install_entry.py` 通过，7 tests。
- `git diff --check` 通过。

## 需求映射

- R11: 已覆盖。公开文档前置说明 workflow-kit 语言无关，Java/Maven/Jar 作为默认 profile，前端和 Go 示例已提供。

## 结论

T09 完成。workflow-kit 的公开叙事已从 Java 默认项目感调整为语言无关工作流包。
