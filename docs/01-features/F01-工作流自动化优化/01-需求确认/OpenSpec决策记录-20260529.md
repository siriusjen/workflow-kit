# OpenSpec 决策记录 — 2026-05-29

## 结论

- feature_id: F01
- mode: formal-change
- change_id: chg-20260529-001
- decision: 本需求改变 workflow-kit 的安装模型、状态机实现方式、门禁执行方式和跨项目配置方式，需要正式 OpenSpec change。
- authoritative_location: `docs/01-features/F01-工作流自动化优化/openspec/changes/chg-20260529-001/`

## 判定依据

- 是否新增能力: 是。新增受管块安装注入脚本、声明式工作流定义、门禁自动校验绑定、上下文包模板化和低摩擦命令能力。
- 是否破坏 API / 数据结构: 否。不得改变 S1-S10 阶段数量、门禁顺序和既有 state.json 语义；新增 state 字段必须向后兼容。
- 是否改变架构模式: 是。脚本从多处硬编码迁移到单一事实源和公共模块。
- 是否涉及安全 / 权限 / 性能语义变化: 是。安装过程必须保留目标项目已有入口文件，避免覆盖用户配置；门禁执行必须减少遗漏和状态漂移。
- 是否仅做需求结构化和验收边界抽取: 否。需要实际脚本、测试和文档落地。

## 执行记录

- 已读取 `openspec/AGENTS.md`: 否。当前仓库不存在根目录 `openspec/AGENTS.md`，按项目工作流规范创建 feature-local OpenSpec change。
- structure-only 输出:
  - R 编号: R01-R11
  - 场景: 安装、测试安全网、声明式配置、门禁自动化、状态字段、上下文包、公共工具、配置校验、簿记降摩擦、文档泛化
  - 验收边界: 见 `需求说明书-v1.md` 和 `覆盖检查.json`
- formal-change 输出:
  - authoritative_location: `docs/01-features/F01-工作流自动化优化/openspec/changes/chg-20260529-001/`
  - proposal: `openspec/changes/chg-20260529-001/proposal.md`
  - tasks: `openspec/changes/chg-20260529-001/tasks.md`
  - specs: `openspec/changes/chg-20260529-001/specs/workflow-kit/spec.md`
  - root_mirror_used_for_cli: 否
  - validate command: 未执行，当前仓库没有根 OpenSpec CLI 配置
  - validate result: 不适用；后续以 feature-local 文档和项目 validators 为权威验收

## 关键结论

- 安装模型必须优先修复，因为当前文档引用不存在的 `开发AGENTS.md` / `开发CLAUDE.md`，且 `cp` 会覆盖目标项目入口文件。
- 安装注入脚本不得依赖安装后仍能访问 workflow-kit 源根目录；受管块来源必须随 `.workflow` 一起迁移，或显式传入源路径。
- 代码改动必须在独立 git worktree 执行；A 项包含脚本和测试，不得视为纯文档任务。
- 重构必须先补回归测试，再抽单一事实源、自动门禁和声明式前置。
