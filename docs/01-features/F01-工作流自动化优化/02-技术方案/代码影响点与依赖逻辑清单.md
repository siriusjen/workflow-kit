# 代码影响点与依赖逻辑清单

## 入口与安装

- `AGENTS.md`: 增加 `WORKFLOW-KIT` 受管块边界，保留顶部 OpenSpec 受管块。
- `CLAUDE.md`: 同步增加 `WORKFLOW-KIT` 受管块边界。
- `docs/.workflow/templates/entry/`: 新增可迁移入口模板。
- `docs/.workflow/scripts/install_entry.py`: 新增入口注入脚本。
- `INSTALL.md`、`使用指南.md`、`docs/.workflow/工作流规范.md`: 更新安装说明和迁移指南。

依赖逻辑: 安装脚本必须只修改目标入口文件的受管块，不接触标记外项目内容。

## 测试

- `docs/.workflow/scripts/tests/test_install_entry.py`: 新增安装脚本测试。
- `docs/.workflow/scripts/tests/test_workflow_gates.py`: 扩展现有门禁行为测试。

依赖逻辑: 后续 B-G 重构必须以这些测试作为安全网。

## 状态机与门禁

- `docs/.workflow/scripts/stage_gates.py`: 读取单一 workflow definition、执行 gate-owned validator、声明式 prereq、低摩擦命令。
- `docs/.workflow/scripts/validators.py`: 校验函数去副作用，CLI 保留只读排查。
- `docs/.workflow/workflow_definition.json`: 新增阶段、门禁、子 Agent、上下文定义。
- `docs/.workflow/scripts/workflow_def.py`: 新增定义加载与校验。

依赖逻辑: 第一阶段必须保持现有 `AUTO_TRANSITIONS`、`APPROVAL_TRANSITIONS` 的行为等价。

## 上下文包

- `docs/.workflow/scripts/context_packets.py`: 改为数据组装 + 模板渲染。
- `docs/.workflow/templates/上下文包.md.tpl`
- `docs/.workflow/templates/bug上下文包.md.tpl`

依赖逻辑: context manifest 更新和快照保存逻辑保持不变。

## 公共工具与配置

- `docs/.workflow/scripts/workflow_common.py`: 新增公共读写、校验、blocking 逻辑。
- `docs/.workflow/scripts/workflow_config.py`: 扩展 build/workflow 配置加载与 schema 校验。
- `docs/.workflow/project_config.json`: 保持默认 Java profile，同时文档说明可覆盖。

依赖逻辑: 旧项目缺配置时不失败；类型错误才失败。

## 文档

- `README.md`
- `INSTALL.md`
- `使用指南.md`
- `docs/.workflow/工作流规范.md`

依赖逻辑: 文档不得再引用不存在的 `开发AGENTS.md` / `开发CLAUDE.md`。
